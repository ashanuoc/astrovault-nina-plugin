# =====================================================================
# Offline secret-scan allowlist  (SCAN-1 / SC5 — Phase 19)
# =====================================================================
# This is the VISIBLE, REVIEWABLE list of exceptions for the offline
# secret scanner (CloudUploadPlugin.Tests/Core/SecurityScanTests.cs).
# Exceptions live HERE, in plain text under code review — NOT buried in
# regex. Keep this list as small as possible; every entry is a hole in
# the gate, so justify each one in a comment.
#
# Format:
#   - Lines starting with '#' are comments and are ignored.
#   - Blank lines are ignored.
#   - Section headers are the two ALL-CAPS markers below; entries belong
#     to whichever section they follow.
#
# Two sections:
#   [EXCLUDED PATHS]   repo-relative path fragments; any file whose
#                      repo-relative path CONTAINS a fragment is skipped
#                      entirely by the scan walk.
#   [APPROVED TOKENS]  substrings that, if present on an otherwise-matched
#                      line, SUPPRESS that match (approved fake examples /
#                      the public production host).
# =====================================================================

[EXCLUDED PATHS]
# The audit recommendation doc legitimately records the security finding
# (it quotes the old staging-IP default as the vulnerability being fixed).
# It is the ONLY docs/ file excluded — every other current doc stays under
# the gate so a future doc cannot reintroduce a raw IP or live key.
docs/PLUGIN-AUDIT-AND-PHASE-19-RECOMMENDATIONS.md

[APPROVED TOKENS]
# Format: TOKEN [:: Rule1,Rule2]. A token WITH a rule scope suppresses ONLY those rules on lines that
# contain it; a token with NO scope is documentation-only and suppresses nothing (WR-03) -- so a real
# raw IP or key co-located with an approved value still trips its own rule. Valid rule names:
# IpLiteralEndpoint, AstrovaultPrefixedKey, BareHighEntropyAstrovaultKey.
#
# The documented fake/example API key used throughout BACKEND-API-SPEC.md (av_live_ prefix +
# obviously-fake body). Scoped to the key-prefix rule.
av_live_abc123def456 :: AstrovaultPrefixedKey
# The shorthand example-key token used in CONTEXT/docs prose (below the 12-char body minimum anyway).
av_live_example :: AstrovaultPrefixedKey
# The public production host. Documentation-only: intentionally present in docs, the python helper
# default, and test fallbacks, and it matches no scan rule -- so it carries NO scope and can never
# blanket-suppress a co-located raw IP or key (WR-03). The raw staging IP is NOT here and remains a violation.
vault.astrospherehub.com
